rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow access to the registrations folder for screenshot uploads
    match /registrations/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
